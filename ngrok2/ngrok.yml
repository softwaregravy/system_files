authtoken: auth
region: us
console_ui: true
web_addr: localhost:4040

tunnels:
  savegraph:
    proto: http
    addr: 3000 
    inspect: true
    subdomain: savegraph

  demeter:
    proto: http
    addr: 5000
    inspect: true
    subdomain: demeter

